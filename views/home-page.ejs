<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>UrbanBro | Home</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Righteous&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        :root {
            --primary: #6c5ce7;
            --secondary: #00cec9;
            --accent: #fd79a8;
            --dark: #2d3436;
            --light: #f5f6fa;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white; min-height: 100vh;
        }
        .container { max-width: 1400px; margin: 0 auto; padding: 2rem; }
        header {
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 3rem; padding: 1rem;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px; backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.2);
        }
        .logo {
            font-family: 'Righteous', cursive; font-size: 2.5rem;
            background: linear-gradient(to right, var(--accent), #fff);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }
        nav ul { display: flex; gap: 2rem; list-style: none; }
        nav a {
            color: white; text-decoration: none; font-weight: 500;
            padding: 0.5rem 1rem; border-radius: 8px; transition: 0.3s;
        }
        nav a:hover { background: rgba(255, 255, 255, 0.2); }

        .hero { text-align: center; margin-bottom: 4rem; padding: 4rem 0; }
        .hero h1 { font-size: 3.5rem; margin-bottom: 1.5rem; line-height: 1.2; }
        .hero p { font-size: 1.2rem; max-width: 700px; margin: 0 auto 2rem; opacity: 0.9; }
        .btn {
            display: inline-block; padding: 1rem 2rem; background: var(--accent);
            color: white; border-radius: 50px; text-decoration: none;
            font-weight: 600; font-size: 1.1rem; transition: 0.3s;
            box-shadow: 0 4px 15px rgba(253, 121, 168, 0.3);
        }
        .btn:hover { transform: translateY(-3px); box-shadow: 0 8px 25px rgba(253, 121, 168, 0.4); }

        .section-title {
            font-size: 2rem; margin-bottom: 2rem; position: relative;
            display: inline-block; text-align: center; width: 100%;
        }
        .section-title::after {
            content: ''; position: absolute; bottom: -10px; left: 50%;
            transform: translateX(-50%); width: 50%; height: 4px;
            background: linear-gradient(to right, var(--accent), var(--secondary));
            border-radius: 2px;
        }

        .slider {
            display: flex; align-items: center; justify-content: center;
            gap: 1rem; margin-bottom: 4rem; position: relative;
        }
        .slider-container {
            display: flex; gap: 1.5rem;
            /* Changed overflow to allow scrolling on mobile */
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            width: 100%;
            padding: 1rem;
            /* Hide scrollbar for a cleaner look */
            scrollbar-width: none; /* Firefox */
            -ms-overflow-style: none;  /* IE and Edge */
        }
        .slider-container::-webkit-scrollbar {
            display: none; /* Chrome, Safari, and Opera */
        }
        .product-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 12px; padding: 1.2rem;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: 0.3s; text-align: center;
            display: flex; flex-direction: column; justify-content: space-between;
            min-height: 350px; width: 305px; flex: 0 0 305px; /* Fixed width for cards */
            color: var(--dark);
            scroll-snap-align: center;
        }
        .product-card:hover { transform: translateY(-5px); box-shadow: 0 12px 25px rgba(0, 0, 0, 0.15); }
        .product-image {
            width: 100%; height: 160px; object-fit: contain;
            border-radius: 8px; margin-bottom: 1rem; background-color: #f8f9fa; padding: 8px;
        }
        .product-name {
            font-size: 1.1rem; font-weight: 600; color: var(--dark);
            height: 3.1rem; overflow: hidden; display: -webkit-box;
            -webkit-line-clamp: 2; -webkit-box-orient: vertical;
        }
        .product-price { font-weight: 700; color: var(--accent); font-size: 1.3rem; margin: 0.5rem 0; }

        footer {
            text-align: center; padding: 2rem; margin-top: 4rem;
            background: rgba(255, 255, 255, 0.1); border-radius: 15px; backdrop-filter: blur(10px);
        }
        .social-links { display: flex; justify-content: center; gap: 1.5rem; margin: 1rem 0; }
        .social-links a { color: white; font-size: 1.5rem; transition: 0.3s; }
        .social-links a:hover { color: var(--accent); transform: translateY(-3px); }

        .no-products { text-align: center; padding: 2rem; font-size: 1.2rem; width: 100%; color: white; }

        /* Responsive Styles */
        @media (max-width: 992px) {
            .container { padding: 1rem; }
            header { flex-direction: column; gap: 1rem; }
            .hero { padding: 2rem 0; }
            .hero h1 { font-size: 2.5rem; }
            .hero p { font-size: 1.1rem; padding: 0 1rem; }
            .product-card { width: 280px; flex: 0 0 280px; }
        }

        @media (max-width: 576px) {
            nav ul { flex-wrap: wrap; justify-content: center; gap: 0.5rem 1rem;}
            .logo { font-size: 1.8rem; }
            .hero h1 { font-size: 2rem; }
            .hero p { font-size: 1rem; }
            .product-card { width: 80vw; flex: 0 0 80vw; }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">Urban Commerce</div>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/products">Shop</a></li>
                    <li><a href="/register">Create Account</a></li>
                    <li><a href="/contact-user-page">Contact</a></li>
                </ul>
            </nav>
        </header>

        <section class="hero">
            <h1>Welcome to the Funkiest Shopping Experience</h1>
            <p>Discover amazing products at unbeatable prices. Our curated collection is designed to bring joy to your everyday life.</p>
            <a href="/products" class="btn">Shop Now</a>
        </section>

        <section class="featured">
            <h2 class="section-title">Featured Products</h2>
            <div class="slider">
                <div class="slider-container">
                    
                    <% if (products && products.length > 0) { %>
                        <% products.forEach(product => { %>
                            <a href="/products/<%= product._id %>" class="product-card">
                                <img src="<%= product.url %>" class="product-image" alt="<%= product.name %>">
                                <div>
                                    <h3 class="product-name"><%= product.name %></h3>
                                    <p class="product-price">₹<%= product.price.toLocaleString() %></p>
                                </div>
                            </a>
                        <% }) %>
                    <% } else { %>
                        <div class="no-products">
                            <p>✨ No products to display at the moment. Please check back later!</p>
                        </div>
                    <% } %>
                    </div>
            </div>
        </section>
    </div>

    <footer>
        <div class="social-links">
            <a href="#"><i class="fab fa-facebook"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-pinterest"></i></a>
        </div>
        <p>&copy; 2025 UrbanCommerce. All rights reserved By PRATHAM SARRAF.</p>
    </footer>
</body>
</html>
